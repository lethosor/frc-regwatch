[project]
name = "frc-regwatch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "gitdb>=4.0.12",
    "gitpython>=3.1.45",
]
